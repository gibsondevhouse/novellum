# Task 02 — Checklist

- [x] Port 5173 is free before starting
- [x] `pnpm desktop:dev` finishes the first Cargo build
- [x] Tauri window opens titled "Novellum" at 1400×900
- [x] Landing page renders inside the WebView
- [-] Editing `src/routes/+page.svelte` hot-reloads
- [-] DevTools attach (right-click → Inspect / F12)
- [x] A `/api/*` route responds successfully
- [x] Closing the window exits the `pnpm desktop:dev` terminal
      cleanly with no orphan processes (`ps aux | grep tauri`)
