# Part Checklist

## Implementation

- [x] `SidebarPrimaryNav.svelte` Muse item label updated
- [x] `SidebarFooterLinks.svelte` Muse link label + aria-label updated
- [x] `AppHeader.svelte` displayTitle fallback for `/nova` → "Muse"

## Post-Implementation

- [x] `pnpm check:tokens` clean
- [x] `pnpm check` clean
- [x] `pnpm lint` clean
- [x] `pnpm lint:css` clean
- [x] `pnpm test` 1059/1059 passing
- [ ] `NovaPanel` visual polish (candle handle, brass border tint) —
      deferred to a follow-up part under this phase
