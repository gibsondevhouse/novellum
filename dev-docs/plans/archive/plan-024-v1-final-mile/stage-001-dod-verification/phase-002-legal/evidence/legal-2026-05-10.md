# Legal — DoD Verification Evidence — 2026-05-10

> **Phase:** stage-001 / phase-002 (Legal)
> **Source checklist:** [`v1-dod-checklist.md` § Legal](../../../../../qa-docs/v1-dod-checklist.md)

## DoD Items in this category

| # | DoD line | Status | Evidence |
|---|---|---|---|
| L1 | `LICENSE` file exists at repository root | ✅ pass | `ls <repo>/.claude/worktrees/distracted-blackburn-61b228/LICENSE` returns the file (978 bytes). Pre-existing. |
| L2 | `EULA.md`, `TERMS.md`, `PRIVACY.md`, `SECURITY.md`, `NOTICE.md` exist at repository root | ✅ pass | All five present (verified by `ls -la *.md` on root). Pre-existing. |
| L3 | `/settings/legal` route renders EULA and NOTICE content | ✅ **closed by this phase** | `src/routes/settings/legal/+page.svelte` (new). Builds cleanly to `.svelte-kit/output/server/entries/pages/settings/legal/_page.svelte.js`; bundled artifact contains both `EULA_default` and `NOTICE_default` strings (verified by `awk` against the build output). |
| L4 | `/settings/about` shows the correct `APP_VERSION` and a link to `/settings/legal` | ✅ **link closed by this phase** | `src/routes/settings/about/+page.svelte:50-57` adds a "Legal" detail row with `<a href="/settings/legal">`. Version display was already wired (chain: `src/lib/version.ts` → `src/routes/api/settings/about/+server.ts` → about page). |
| L5 | `NOTICE.md` lists all seven key third-party packages | ✅ pass | `NOTICE.md:15-26` lists 10 packages: SvelteKit, Svelte, TipTap, better-sqlite3, Tauri, Vite, Tailwind CSS, TypeScript, Vitest, Playwright. ≥7 key packages present. Pre-existing. |

All five Legal DoD line items now satisfied.

## Implementation evidence

### Files

```text
src/routes/settings/legal/+page.svelte                                     (new, 75 LOC)
src/routes/settings/+layout.svelte                                         (updated, +1 LOC)
src/routes/settings/about/+page.svelte                                     (updated, ~10 LOC)
```

### Key code citations

**Drift-proof content sourcing** —
[src/routes/settings/legal/+page.svelte:1-5](../../../../../../src/routes/settings/legal/+page.svelte):

```svelte
<script lang="ts">
  import SurfaceCard from '$lib/components/ui/SurfaceCard.svelte';
  import eulaText from '../../../../EULA.md?raw';
  import noticeText from '../../../../NOTICE.md?raw';
</script>
```

The Vite `?raw` suffix bundles the .md text into the page's chunk at
build time. No inline duplication. Future edits to the canonical
`EULA.md` or `NOTICE.md` flow into the UI on next build with no
separate sync step.

**PillNav wiring** —
[src/routes/settings/+layout.svelte:9-21](../../../../../../src/routes/settings/+layout.svelte):

```svelte
const items: PillNavItem[] = [
  { id: 'appearance', label: 'Appearance' },
  { id: 'defaults', label: 'Defaults' },
  { id: 'shortcuts', label: 'Shortcuts' },
  { id: 'ai', label: 'AI' },
  { id: 'data', label: 'Data' },
  { id: 'backup', label: 'Backup' },
  { id: 'export-defaults', label: 'Export Defaults' },
  { id: 'privacy', label: 'Privacy' },
  { id: 'updates', label: 'Updates' },
  { id: 'about', label: 'About' },
  { id: 'legal', label: 'Legal' },
];
```

**About → Legal link** —
[src/routes/settings/about/+page.svelte:50-57](../../../../../../src/routes/settings/about/+page.svelte):

```svelte
<div class="about__detail-row">
  <dt class="about__label">Legal</dt>
  <dd class="about__value">
    <a class="about__link" href="/settings/legal">
      View EULA &amp; third-party notices
    </a>
  </dd>
</div>
```

### Gate verification

| Gate | Result |
| --- | --- |
| `pnpm check` | `COMPLETED 1598 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS` |
| `pnpm build` | `✓ built in 3.34s`; legal route emitted; bundled .md text confirmed |
| `pnpm check:tokens` | `✓ Token enforcement: 312 files scanned, 0 violations.` |
| `npx stylelint` (3 changed files) | exit 0 |
| `npx eslint` (3 changed files) | exit 0 |

### Build-output proof of bundled content

```text
$ awk '/Grant of License|Effective Date|Third-Party Notices/{print FILENAME":"NR":"substr($0,1,80)}' \
  .svelte-kit/output/server/entries/pages/settings/legal/_page.svelte.js
.svelte-kit/output/.../legal/_page.svelte.js:4:var EULA_default = "# End User License Agreement (EULA)\n\n<!-- LEGAL_REVIEW_REQ
.svelte-kit/output/.../legal/_page.svelte.js:7:var NOTICE_default = "# Third-Party Notices\n\nNovellum includes or depends on t
```

## Notes & follow-ups

- The Privacy page (`/settings/privacy`) still uses the older inline
  duplication pattern (F6 in the triage). The exact same `?raw`
  approach used here resolves it; recommended for phase-007 (Trust
  and Data) so it lands with the other privacy/backup work.
- Plain `<pre>` rendering of the .md was chosen over adding a
  markdown library because no markdown renderer is currently a
  dependency. The text is readable as-is. Upgrading to a parsed
  HTML render is a follow-up if/when a markdown library lands.
- No additional tests added. The existing test suite continues to
  pass (modulo the pre-existing `.claude/worktrees/` discovery
  noise documented in `phase-001/evidence/gate-baseline-2026-05-10.md`).
