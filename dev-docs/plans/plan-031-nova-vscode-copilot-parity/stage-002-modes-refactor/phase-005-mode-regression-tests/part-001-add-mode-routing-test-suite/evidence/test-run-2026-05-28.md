# Mode Regression Tests — 2026-05-28

## Test File

`tests/nova/mode-routing.test.ts`

## Results

```
 ✓ tests/nova/mode-routing.test.ts (11 tests)

Nova mode routing — sendNovaChat
  ✓ ask mode streams through the chat path without calling pipeline runner
  ✓ ask mode defaults when mode is omitted
  ✓ write mode + outline request routes to pipeline runner
  ✓ write mode + unsupported concrete request appends unsupported-action state
  ✓ write mode + outline without projectId fails with an error message
  ✓ agent mode returns a guarded stub message without streaming

Nova mode store — per-project persistence
  ✓ defaults to ask when no persisted value exists
  ✓ restores the persisted mode for the same project
  ✓ does not leak mode between different projects
  ✓ normalises invalid persisted values to ask
  ✓ cycles modes in order ask → write → agent → ask
```

## Full Suite

```
Test Files: 191 passed (191)
Tests: 1310 passed (1310)
```

## Regressions Checked

- Ask/Write behavior does not regress existing context grounding (buildRagContext still called for ask and write)
- No test expects direct manuscript mutation
- All old Chat/Scribe literals removed; TypeScript would catch any re-introduction
