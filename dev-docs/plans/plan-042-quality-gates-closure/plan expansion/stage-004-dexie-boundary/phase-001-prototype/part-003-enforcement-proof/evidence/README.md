# evidence/

This folder is for append-only artifacts:
- command output (`pnpm check`, `pnpm lint`, `pnpm lint:css`, `pnpm test`, `pnpm check:tokens`, `pnpm test:visual`)
- warning inventories
- visual drift ledger
- Dexie import inventories

Do not delete files; add new artifacts with date-stamped names.
