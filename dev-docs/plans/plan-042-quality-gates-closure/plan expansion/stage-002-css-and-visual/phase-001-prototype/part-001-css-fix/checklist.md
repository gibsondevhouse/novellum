# checklist.md

- [ ] run `pnpm lint:css` and capture output
- [ ] fix `IndividualsWorkspaceShell.svelte` lint failures (no `!important` unless proven necessary)
- [ ] re-run `pnpm lint:css` and capture clean output
